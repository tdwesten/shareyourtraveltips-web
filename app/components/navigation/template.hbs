<nav class='border-b border-orange-300 border-opacity-25 lg:border-none'>
  <div class='px-2 mx-auto sm:px-4 lg:px-8'>
    <div class='relative flex items-center justify-between h-16'>
      <div class='flex items-center px-2 lg:px-0'>
        <div class='flex-shrink-0'>
          <LinkTo @route='index'>
            <h1 class='text-lg font-semibold text-white font-headings'>
              ShareYourTravel.tips
              <span class='uppercase opacity-75'>
                Béta
              </span>
            </h1>
          </LinkTo>
        </div>
        <div class='hidden lg:block lg:ml-10'>
          <div class='flex space-x-4'>
            {{#if this.session.isAuthenticated}}
              {{#each this.getMenuItems as |item|}}
                <LinkTo
                  @route={{item.route}}
                  @current-when={{item.currentWhen}}
                  @activeClass='bg-orange-700 text-white'
                  class='px-3 py-2 font-medium text-white rounded-md hover:bg-orange-500 font-headings hover:bg-opacity-75'
                >
                  {{t item.title}}
                </LinkTo>
              {{/each}}
            {{/if}}
          </div>
        </div>
      </div>

      <div class='lg:block lg:ml-4'>
        <div class='flex space-x-4'>
          {{#if this.session.isAuthenticated}}
            <span
              class='hidden px-3 py-2 text-sm italic text-white md:inline font-headings'
            >
              {{t 'menu.welcome_message' name=this.currentUser.user.firstName}}
            </span>
            <LinkTo
              @route='logout'
              @activeClass='bg-orange-700 text-white'
              class='text-white hover:bg-orange-500 hover:bg-opacity-75 rounded-md py-1.5 px-4 font-medium font-headings bg-orange-600 border border-white'
            >
              {{t 'menu.logout'}}
            </LinkTo>
          {{else}}
            <LinkTo
              @route='register'
              @activeClass='bg-orange-700 text-white'
              class='text-white hover:bg-orange-500 hover:bg-opacity-75 rounded-md py-1.5 px-4 font-medium font-headings hidden md:inline-block'
            >
              {{t 'menu.register'}}
            </LinkTo>
            <LinkTo
              @route='login'
              @activeClass='bg-orange-700 text-white'
              class='text-white hover:bg-orange-500 hover:bg-opacity-75 rounded-md py-1.5 px-4 font-medium font-headings bg-orange-600 border border-white'
            >
              {{t 'menu.login'}}
            </LinkTo>
          {{/if}}
        </div>
      </div>

    </div>
  </div>
</nav>