<div
  class='bg-white border-t border-gray-200 max-h-full overflow-x-scroll overflow-y-auto flex-shrink-1'
>

  <ul role='list' class='overflow-y-scroll flex-1'>
    {{#each @tips as |tip|}}
      <TipList::Item @model={{tip}} {{on 'click' (fn @onOpen tip)}} />
    {{/each}}
  </ul>
</div>